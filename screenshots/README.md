# Screenshots

Place your screenshots here with these filenames so the README embeds resolve:

| Filename | Content |
|---|---|
| `grafana-dashboard.png` | DNS security dashboard — block rate gauge, total queries, blocked vs allowed |
| `adblock-live.png` | Terminal: live NXDOMAIN log + adblock status + `wg show` (keys redacted!) |
| `prometheus-targets.png` | Prometheus targets page, both jobs UP (optional) |

**Before committing screenshots, verify none of them contain:**
- WireGuard private or public keys
- DuckDNS token
- Tailscale IPs, if you consider your tailnet private
